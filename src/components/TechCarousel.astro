---
---

<div
    id="tech-carousel"
    class="relative overflow-hidden"
    style="margin: 0 calc(-1 * clamp(1rem, 5vw, 6rem));"
>
    <canvas
        id="tech-carousel-canvas"
        class="block w-full"
        style="height: clamp(500px, 55vw, 900px);"
        aria-hidden="true"
    ></canvas>
</div>

<script>
    window.addEventListener('models:hero-ready', () => {
        import('../scripts/tech-carousel-scene');
    }, { once: true });
</script>
